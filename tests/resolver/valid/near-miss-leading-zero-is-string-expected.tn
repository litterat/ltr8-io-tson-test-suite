!!id:"https://tson.io/test-suite/resolver/valid/near-miss-leading-zero-is-string-expected.tn"
{
  spec: "§4.4"
  description: "A leading zero fails the number grammar, so the token falls through to string (spec's own example)"
  outcome: valid
  base-value: {
    kind: string
    text: "007"
  }
}
