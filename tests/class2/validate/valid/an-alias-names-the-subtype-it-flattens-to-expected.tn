!!id:"https://tson.io/test-suite/class2/validate/valid/an-alias-names-the-subtype-it-flattens-to-expected.tn"
!!schema:"https://tson.io/test-suite/schemas/validate-sidecar.tn"
{
  spec: "[TSON-SCHEMA] §7.2, [TSON-SCHEMA] §8.3"
  description: "§7.2 compares after reference flattening of BOTH sides, so an alias of a subtype names that subtype at a position typed by its base. The value carries the subtype's own field, so a processor admitting the tag but reading the base's field set reports it as unrecognised instead."
  schema: "fixtures/validate-alias.tn"
  valid: _
}
