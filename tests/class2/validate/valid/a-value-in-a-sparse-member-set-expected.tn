!!id:"https://tson.io/test-suite/class2/validate/valid/a-value-in-a-sparse-member-set-expected.tn"
!!schema:"https://tson.io/test-suite/schemas/validate-sidecar.tn"
{
  spec: "[TSON-SCHEMA] §5.6"
  description: "A member of a sparse `members` set at each exact tier. The facet composes with the family's other constraints rather than replacing them, so the ordinary case is simply that a member is admitted."
  schema: "fixtures/validate-ports.tn"
  valid: _
}
